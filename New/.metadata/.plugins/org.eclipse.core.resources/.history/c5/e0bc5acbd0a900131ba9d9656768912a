package newExam;

import static org.junit.Assert.*;
import java.util.*;

import org.junit.Test;

public class RoomTest {

	@Test
	public void test() {
		Date date = new Date();
		ExamTimetableInfo exam = new ExamTimetableInfo(7, date);
		Room room = new Room("A100", RoomType.COMPUTER_CLUSTER, 50);
		room.setTimetable(exam.getExamDuration(), exam.getStartDate());
		System.out.println(room.getTimetable());
		
		for(Day d: room.getTimetable()) {
			for (Hour h: d.getHours()) {
				
			}
		}
	}

}
